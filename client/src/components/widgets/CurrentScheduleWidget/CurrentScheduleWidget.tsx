/** Stub — will be implemented when schedule widget is built. */
export function CurrentScheduleWidget({ selectedDate: _sd, onDateChange: _odc }: { selectedDate: Date; onDateChange: (d: Date) => void }) {
  return <div className="text-gray-500 text-sm p-4">Current Schedule — not yet implemented</div>;
}
