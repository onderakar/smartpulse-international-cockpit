/** Stub — will be implemented when market price integration is built. */
export function MarketPriceWidget({ selectedDate: _sd, onDateChange: _odc }: { selectedDate: Date; onDateChange: (d: Date) => void }) {
  return <div className="text-gray-500 text-sm p-4">Market Prices — not yet implemented</div>;
}
