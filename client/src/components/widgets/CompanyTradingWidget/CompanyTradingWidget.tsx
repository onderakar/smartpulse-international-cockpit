/** Stub — will be implemented when trading integration is built. */
export function CompanyTradingWidget({ selectedDate: _sd, onDateChange: _odc }: { selectedDate: Date; onDateChange: (d: Date) => void }) {
  return <div className="text-gray-500 text-sm p-4">Company Trading — not yet implemented</div>;
}
